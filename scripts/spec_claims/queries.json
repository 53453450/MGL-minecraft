{
  "suite": "queries",
  "spec": "OpenGL 4.6 Core",
  "spec_source": "external/OpenGL-Registry/specs/gl/glspec46.core.pdf",
  "scope_note": "Occlusion/timer/transform-feedback overflow queries.",
  "claims": [
    {
      "id": "begin_query_implemented",
      "claim": "BeginQuery / BeginQueryIndexed are implemented entry points that activate a query object rather than always calling mgl_unimplemented.",
      "code_locus": "MGL/src/mgl_gl_extensions.c:mglBeginQuery",
      "code_excerpt": "void mglBeginQuery(...) { mglBeginQueryAtIndex(ctx, target, 0u, id); }",
      "spec_section": "4.2",
      "spec_quote": "QUERY OBJECTS AND ASYNCHRONOUS QUERIES 44 Errors An INVALID_VALUE error is generated if n is negative.",
      "evidence": "eries only, but no object is associated with them until the first time they are used by BeginQuery, BeginQueryIndexed, or QueryCounter (see section 4.3). OpenGL 4.6 (Core Profile) - May 5, 2022 4.2. QUERY OBJECTS AND ASYNCHRONOUS QUERIES 44 Errors An INVALID_VALUE error is generated if n is negative. Query objects may also be created with the command void CreateQueries( enum target, sizei n, uint *ids ); CreateQueries returns n previously unused query object names in ids, each representing a new query object with the specified target. target must be one of the query object targets described in",
      "evidence_verified": true
    },
    {
      "id": "gen_queries_n_negative",
      "claim": "GenQueries generates GL_INVALID_VALUE when n is negative (sizei rule).",
      "code_locus": "MGL/src/mgl_gl_extensions.c GenQueries",
      "code_excerpt": "negative n → INVALID_VALUE",
      "spec_section": "4.2",
      "spec_quote": "An INVALID_VALUE error is generated if n is negative.",
      "evidence": "re Profile) - May 5, 2022 4.2. QUERY OBJECTS AND ASYNCHRONOUS QUERIES 44 Errors An INVALID_VALUE error is generated if n is negative. Query objects may also be created with the command void CreateQueries( enum target, sizei n, uint *ids ); CreateQueries returns n previously unused query object names in ids, each representing a new query object with the specified target. target must be one of the query object targets described in section 4.2.1. The initial state of the resulting query object is that the result is marked available (the value of QUER",
      "evidence_verified": true
    }
  ],
  "spec_corpus": [
    "scratch/spec_text/glspec46.core.norm.txt",
    "scratch/spec_text/GLSLangSpec.4.60.norm.txt"
  ]
}
