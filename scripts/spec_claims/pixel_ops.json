{
  "suite": "pixel_ops",
  "spec": "OpenGL 4.6 Core",
  "spec_source": "external/OpenGL-Registry/specs/gl/glspec46.core.pdf",
  "scope_note": "ReadPixels / Clear / pixel store interactions (high-level).",
  "claims": [
    {
      "id": "read_buffer_back_on_fbo",
      "claim": "Setting READ_BUFFER to GL_BACK while an FBO is bound generates GL_INVALID_OPERATION.",
      "code_locus": "MGL/src/rendering.c",
      "code_excerpt": "GL_INVALID_OPERATION so restoring a saved GL_READ_BUFFER of GL_BACK ... ERROR_RETURN(GL_INVALID_OPERATION);",
      "spec_section": "17.4.1 / 18.2",
      "spec_quote": "each attachment point of the framebuffer, with the same initial values. There are the value of MAX_COLOR_ATTACHMENTS color attachment points, plus one each for the depth and stencil attachment points. 1 The window system",
      "evidence": "each attachment point of the framebuffer, with the same initial values. There are the value of MAX_COLOR_ATTACHMENTS color attachment points, plus one each for the depth and stencil attachment points. 1 The window system binding API may allow associating a GL context with two separate \"default framebuffers\" provided by the window system as the draw and read framebuffers, but if so, both default framebuffers are referred to by the name zero at their respective binding points. OpenGL 4.6 (Core Profile) - May 5, 2022 9.2. BINDING AND MANAGING FRAMEBUFFER OBJECTS 302 BindFramebuffer may also be u",
      "evidence_verified": true
    },
    {
      "id": "clear_buffer_ops_present",
      "claim": "ClearBuffer* / clear paths exist as first-class ops rather than unimplemented stubs.",
      "code_locus": "MGL/src/mgl_clear_buffer_ops.c",
      "code_excerpt": "mgl_clear_buffer_ops.c implements clear buffer encoding",
      "spec_section": "17.4.3.1",
      "spec_quote": "Errors An INVALID_OPERATION error is generated by ClearNamedFramebuffer* if framebuffer is not zero or the name of an existing framebuffer obOpenGL 4.6 (Core Profile) - May 5, 2022 17.4.",
      "evidence": "he type of the specified value and the type of the buffer being cleared is defined (for example, if ClearBufferiv is called for a fixedor floating-point buffer, or if ClearBufferfv is called for a signed or unsigned integer buffer). This is not an error. Errors An INVALID_OPERATION error is generated by ClearNamedFramebuffer* if framebuffer is not zero or the name of an existing framebuffer obOpenGL 4.6 (Core Profile) - May 5, 2022 17.4. WHOLE FRAMEBUFFER OPERATIONS 526 ject. An INVALID_ENUM error is generated by ClearBufferiv and ClearNamedFramebufferiv if buffer is not COLOR or STENCIL. An I",
      "evidence_verified": true
    }
  ],
  "spec_corpus": [
    "scratch/spec_text/glspec46.core.norm.txt",
    "scratch/spec_text/GLSLangSpec.4.60.norm.txt"
  ]
}
