{
  "suite": "buffers",
  "spec": "OpenGL 4.6 Core",
  "spec_source": "external/OpenGL-Registry/specs/gl/glspec46.core.pdf",
  "scope_note": "Buffer objects, BindBufferRange, MapBufferRange, ClearBufferSubData.",
  "claims": [
    {
      "id": "bind_buffer_range_negative_offset",
      "claim": "BindBufferRange generates GL_INVALID_VALUE when offset is negative.",
      "code_locus": "MGL/src/buffers.c:mglBindBufferRange",
      "code_excerpt": "if (offset < 0) { ... ERROR ... INVALID_VALUE }",
      "spec_section": "6.7.1",
      "spec_quote": "An INVALID_VALUE error is generated by BindBufferRange if offset is negative.",
      "evidence": "all to GenBuffers, or if such a name has since been deleted with DeleteBuffers. An INVALID_VALUE error is generated by BindBufferRange if offset is negative. An INVALID_VALUE error is generated by BindBufferRange if buffer is non-zero and size is less than or equal to zero. An INVALID_VALUE error is generated by BindBufferRange if buffer is non-zero and offset or size do not respectively satisfy the constraints described for those parameters for the specified target, as described in section 6.7.1. The commands void BindBuffersBase( enum target, uint first, sizei count, O",
      "evidence_verified": true
    },
    {
      "id": "bind_buffer_range_nonpositive_size",
      "claim": "BindBufferRange generates GL_INVALID_VALUE when buffer is non-zero and size is less than or equal to zero.",
      "code_locus": "MGL/src/buffers.c:mglBindBufferRange",
      "code_excerpt": "if (size <= 0) { fprintf... ERROR INVALID_VALUE }",
      "spec_section": "6.7.1",
      "spec_quote": "An INVALID_VALUE error is generated by BindBufferRange if buffer is non-zero and size is less than or equal to zero.",
      "evidence": ". An INVALID_VALUE error is generated by BindBufferRange if offset is negative. An INVALID_VALUE error is generated by BindBufferRange if buffer is non-zero and size is less than or equal to zero. An INVALID_VALUE error is generated by BindBufferRange if buffer is non-zero and offset or size do not respectively satisfy the constraints described for those parameters for the specified target, as described in section 6.7.1. The commands void BindBuffersBase( enum target, uint first, sizei count, OpenGL 4.6 (Core Profile) - May 5, 2022 6.1. CREATING AND BINDING BUFFER OBJECTS 64 const uint *buffers ); void BindBu",
      "evidence_verified": true
    },
    {
      "id": "map_buffer_range_negative_offset_length",
      "claim": "MapBufferRange generates GL_INVALID_VALUE when offset or length is negative.",
      "code_locus": "MGL/src/buffers.c:mglMapBufferRange",
      "code_excerpt": "if (offset < 0) ERROR; if (length < 0) ERROR; → INVALID_VALUE",
      "spec_section": "6.3",
      "spec_quote": "An INVALID_VALUE error is generated if offset or length is negative, if offset + length is greater than the value of BUFFER_SIZE.",
      "evidence": "enGL 4.6 (Core Profile) - May 5, 2022 6.3. MAPPING AND UNMAPPING BUFFER DATA 76 An INVALID_VALUE error is generated if offset or length is negative, if offset + length is greater than the value of BUFFER_SIZE, or if access has any bits set other than those defined above. An INVALID_OPERATION error is generated for any of the following conditions: • length is zero. • The buffer is already in a mapped state. • Neither MAP_READ_BIT nor MAP_WRITE_BIT is set. • MAP_READ_BIT is set and any of MAP_INVALIDATE_RANGE_BIT, MAP_INVALIDATE_BUFFER_BIT, or MAP_UNSYNCHRONIZED_BIT",
      "evidence_verified": true
    },
    {
      "id": "map_already_mapped",
      "claim": "Mapping a buffer that is already mapped generates GL_INVALID_OPERATION.",
      "code_locus": "MGL/src/buffers.c",
      "code_excerpt": "ERROR_CHECK_RETURN_VALUE(ptr->mapped == false, GL_INVALID_OPERATION, NULL);",
      "spec_section": "6.3",
      "spec_quote": "An INVALID_OPERATION error is generated if any of the following conditions occur: The buffer is already in a mapped state.",
      "evidence": "value less than zero or greater than or equal to the value of MAX_IMAGE_UNITS. An INVALID_OPERATION error is generated if any of the following conditions occur: • the size indicated in the name of the Uniform* command used does not match the size of the uniform declared in the shader, • the component type and count indicated in the name of the Uniform* command used does not match the type of the uniform declared in the shader, where a boolean uniform component type is considered to match any of the Uniform*i{v}, Uniform*ui{v}, or Uniform*f{v} OpenGL 4.6 (Core Profi",
      "evidence_verified": true
    },
    {
      "id": "clear_buffer_subdata_bad_format",
      "claim": "ClearBufferSubData rejects an unrecognized pixel format with GL_INVALID_VALUE (or INVALID_ENUM for target).",
      "code_locus": "MGL/src/buffers.c ClearBufferSubData validation",
      "code_excerpt": "switch(format) default: ERROR_RETURN_VALUE(GL_INVALID_VALUE, false);",
      "spec_section": "6.2",
      "spec_quote": "An INVALID_VALUE error is generated if format is not one of the formats listed ... An INVALID_ENUM error is generated by ClearBufferSubData if target is not one of the generic buffer binding targets.",
      "evidence": "layer is negative, or if texture is not the name of an existing texture object. An INVALID_VALUE error is generated if format is not one of the formats listed in table 8.26. The command void BindImageTextures( uint first, sizei count, const uint *textures ); binds count existing texture objects to image units numbered first through first + count - 1. If textures is not NULL, it specifies an array of count values, each of which must be zero or the name of an existing texture object. If textures is NULL, each affected image unit from first through first + count - 1 will be",
      "evidence_verified": true
    }
  ],
  "spec_corpus": [
    "scratch/spec_text/glspec46.core.norm.txt",
    "scratch/spec_text/GLSLangSpec.4.60.norm.txt"
  ]
}
