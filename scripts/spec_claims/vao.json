{
  "suite": "vao",
  "spec": "OpenGL 4.6 Core Profile (May 5, 2022)",
  "spec_source": "external/OpenGL-Registry/specs/gl/glspec46.core.pdf",
  "scope_note": "Vertex array objects, attribute format/binding, BindVertexBuffer(s).",
  "claims": [
    {
      "id": "gen_vao_n_negative",
      "claim": "GenVertexArrays / DeleteVertexArrays generate GL_INVALID_VALUE when n is negative.",
      "code_locus": "MGL/src/vertex_arrays.c:mglDeleteVertexArrays",
      "code_excerpt": "if (n < 0) { ERROR_RETURN(GL_INVALID_VALUE); return; }",
      "spec_section": "10.3.1",
      "spec_quote": "An INVALID_VALUE error is generated if n is negative.",
      "evidence": "re Profile) - May 5, 2022 4.2. QUERY OBJECTS AND ASYNCHRONOUS QUERIES 44 Errors An INVALID_VALUE error is generated if n is negative. Query objects may also be created with the command void CreateQueries( enum target, sizei n, uint *ids ); CreateQueries returns n previously unused query object names in ids, each representing a new query object with the specified target. target must be one of the query object targets described in section 4.2.1. The initial state of the resulting query object is that the result is marked available (the value of QUER",
      "evidence_verified": true
    },
    {
      "id": "bind_vao_unknown_name",
      "claim": "BindVertexArray generates GL_INVALID_OPERATION when array is not zero and is not a name returned by GenVertexArrays/CreateVertexArrays (or was deleted).",
      "code_locus": "MGL/src/vertex_arrays.c:mglBindVertexArray",
      "code_excerpt": "if (array <= STATE(vao_table).current_name) { ptr = newVAO(...); } else { ERROR_RETURN(GL_INVALID_OPERATION); return; }",
      "spec_section": "10.3.1",
      "spec_quote": "An INVALID_OPERATION error is generated if array is not zero or a name returned from a previous call to CreateVertexArrays or GenVertexArrays, or if such a name has since been deleted with DeleteVertexArrays.",
      "evidence": "nd DrawElements; and all queries of vertex array state (see chapter 22). Errors An INVALID_OPERATION error is generated if array is not zero or a name returned from a previous call to CreateVertexArrays or GenVertexArrays, or if such a name has since been deleted with DeleteVertexArrays. An INVALID_OPERATION error is generated by any commands which modify, draw from, or query vertex array state when no vertex array is bound. This occurs in the initial GL state, and may occur as a result of BindVertexArray or a side effect of DeleteVertexArrays. Vertex array objects may also be created with the command void CreateVertexArrays( sizei n, uint *arrays ); CreateVertexArrays returns n previously unused ve",
      "evidence_verified": true
    },
    {
      "id": "delete_bound_vao_unbinds",
      "claim": "Deleting the currently bound VAO rebinds VAO zero (no vertex array object is bound).",
      "code_locus": "MGL/src/vertex_arrays.c:mglDeleteVertexArrays",
      "code_excerpt": "if (ptr == STATE(vao)) { mglBindVertexArray(ctx, 0); }",
      "spec_section": "10.3.1",
      "spec_quote": "If a vertex array object that is currently bound is deleted, the binding for that object reverts to zero and no vertex array object is bound.",
      "evidence": "vertex array object is deleted it has no contents and its name is again unused. If a vertex array object that is currently bound is deleted, the binding for that object reverts to zero and no vertex array object is bound. Unused names in arrays that have been marked as used for the purposes of GenVertexArrays are marked as unused again. Unused names in arrays are silently ignored, as is the value zero. Errors An INVALID_VALUE error is generated if n is negative. A vertex array object is created by binding a name returned by GenVertexArrays with the command void BindVertexArray( uint array ); array is the vertex array object name. The",
      "evidence_verified": true
    },
    {
      "id": "attrib_binding_requires_vao",
      "claim": "VertexAttribBinding generates GL_INVALID_OPERATION when no vertex array object is bound.",
      "code_locus": "MGL/src/vertex_arrays.c:mglVertexAttribBinding",
      "code_excerpt": "ERROR_CHECK_RETURN(ptr, GL_INVALID_OPERATION);",
      "spec_section": "10.3.2",
      "spec_quote": "An INVALID_OPERATION error is generated if no vertex array object is bound.",
      "evidence": "ndingindex is greater than or equal to the value of MAX_VERTEX_ATTRIB_BINDINGS. An INVALID_OPERATION error is generated if no vertex array object is bound. The one, two, three, or four values in an array that correspond to a single vertex comprise an array element. When size is BGRA, it indicates four values. The values within each array element are stored sequentially in memory. However, if size is BGRA, the first, second, third, and fourth values of each array element are taken from the third, second, first, and fourth values in memory respectively. The commands Open",
      "evidence_verified": true
    },
    {
      "id": "bind_vertex_buffer_requires_vao",
      "claim": "BindVertexBuffer generates GL_INVALID_OPERATION when no vertex array object is bound.",
      "code_locus": "MGL/src/vertex_buffers.c:mglBindVertexBuffer",
      "code_excerpt": "ERROR_CHECK_RETURN(STATE(vao), GL_INVALID_OPERATION);",
      "spec_section": "10.3.2",
      "spec_quote": "An INVALID_OPERATION error is generated by BindVertexBuffer if no vertex array object is bound.",
      "evidence": "bound buffer object, and any previous binding to bindingindex is broken. Errors An INVALID_OPERATION error is generated by BindVertexBuffer if no vertex array object is bound. An INVALID_OPERATION error is generated by VertexArrayVertexBuffer if vaobj is not the name of an existing vertex array object. An INVALID_OPERATION error is generated if buffer is not zero or a name returned from a previous call to GenBuffers or CreateBuffers, or if such a name has since been deleted with DeleteBuffers. An INVALID_VALUE error is generated if bindingindex is greater than or equal to the value of MAX",
      "evidence_verified": true
    },
    {
      "id": "bind_vertex_buffer_creates_gen_name",
      "claim": "BindVertexBuffer accepts a GenBuffers name that has never been bound and creates the buffer object state vector (just as BindBuffer would).",
      "code_locus": "MGL/src/vertex_buffers.c:bindVertexBuffer",
      "code_excerpt": "buf = findBuffer(ctx, buffer); if (!buf && mglBufferNameWasGenerated(ctx, buffer)) { buf = getBuffer(ctx, GL_ARRAY_BUFFER, buffer); }",
      "spec_section": "10.3.2",
      "spec_quote": "If buffer is not the name of an existing buffer object, the GL first creates a new state vector, initialized with a zero-sized memory buffer and comprising all the state and with the same initial values listed in table 6.2, just as for BindBuffer.",
      "evidence": "uffers. If buffer is zero, any buffer object bound to bindingindex is detached. If buffer is not the name of an existing buffer object, the GL first creates a new state vector, initialized with a zero-sized memory buffer and comprising all the state and with the same initial values listed in table 6.2, just as for BindBuffer. buffer is then attached to the specified bindingindex of the vertex array object. When sourcing vertex data from the buffer object, offset specifies the offset in basic machine units of the first element in the vertex buffer. Pointers to the ith and (i + 1)st elements of the array differ by stride basic machine units, the pointer to the (i + 1)st element being greater. If the operation is successful no change is mad",
      "evidence_verified": true
    },
    {
      "id": "bind_vertex_buffers_rejects_uncreated_gen_name",
      "claim": "BindVertexBuffers rejects a non-zero buffer name that is not yet an existing buffer object with GL_INVALID_OPERATION, without creating the object first.",
      "code_locus": "MGL/src/vertex_buffers.c:mglBindVertexBuffers",
      "code_excerpt": "if (buffer && !findBuffer(ctx, buffer)) { ERROR_RETURN(GL_INVALID_OPERATION); continue; }",
      "spec_section": "10.3.2",
      "spec_quote": "If buffer is not the name of an existing buffer object, the GL first creates a new state vector ... just as for BindBuffer. BindVertexBuffers is equivalent ... to BindVertexBuffer(first + i, buffers[i], offsets[i], strides[i]).",
      "evidence": "uffers. If buffer is zero, any buffer object bound to bindingindex is detached. If buffer is not the name of an existing buffer object, the GL first creates a new state vector, initialized with a zero-sized memory buffer and comprising all the state and with the same initial values listed in table 6.2, just as for BindBuffer. buffer is then attached to the specified bindingindex of the vertex array object. When sourcing vertex data from the buffer object, offset specifies the offset in basic machine units of the first element in the vertex buffer. Pointers to the ith and (i + 1)st element",
      "evidence_verified": true
    },
    {
      "id": "bind_vertex_buffer_negative_offset_stride",
      "claim": "BindVertexBuffer generates GL_INVALID_VALUE when offset or stride is negative, or when stride exceeds MAX_VERTEX_ATTRIB_STRIDE.",
      "code_locus": "MGL/src/vertex_buffers.c:bindVertexBuffer",
      "code_excerpt": "ERROR_CHECK_RETURN_VALUE(offset >= 0, GL_INVALID_VALUE, false); ERROR_CHECK_RETURN_VALUE(stride >= 0, GL_INVALID_VALUE, false); ERROR_CHECK_RETURN_VALUE((GLuint)stride <= mglVertexAttribStrideLimit(ctx), GL_INVALID_VALUE, false);",
      "spec_section": "10.3.2",
      "spec_quote": "An INVALID_VALUE error is generated if stride or offset is negative, or if stride is greater than the value of MAX_VERTEX_ATTRIB_STRIDE.",
      "evidence": "ndingindex is greater than or equal to the value of MAX_VERTEX_ATTRIB_BINDINGS. An INVALID_VALUE error is generated if stride or offset is negative, or if stride is greater than the value of MAX_VERTEX_ATTRIB_STRIDE. The source of data for multiple vertex attributes may be determined by attaching multiple existing buffer objects to a vertex array object with the commands void BindVertexBuffers( uint first, sizei count, const uint *buffers, const intptr *offsets, const sizei *strides ); OpenGL 4.6 (Core Profile) - May 5, 2022 10.3. VERTEX ARRAYS 356 void VertexArrayVertexBuffers( uint vaobj, uint first, sizei count, const uint *b",
      "evidence_verified": true
    },
    {
      "id": "vertex_attrib_pointer_negative_stride",
      "claim": "VertexAttribPointer generates GL_INVALID_VALUE when stride is negative.",
      "code_locus": "MGL/src/vertex_arrays.c:mglVertexAttribPointer",
      "code_excerpt": "ERROR_CHECK_RETURN(stride >= 0, GL_INVALID_VALUE);",
      "spec_section": "10.3.2",
      "spec_quote": "An INVALID_VALUE error is generated if stride is negative.",
      "evidence": "tride is specified as zero, then array elements are stored sequentially. Errors An INVALID_VALUE error is generated if stride is negative. An INVALID_VALUE error is generated if stride is greater than the value of MAX_VERTEX_ATTRIB_STRIDE. An INVALID_OPERATION error is generated if no buffer is bound to ARRAY_BUFFER, and pointer is not NULL. An INVALID_OPERATION error is generated if no vertex array object is bound. In addition, any of the errors defined by VertexAttrib*Format and VertexAttribBinding may be generated if the parameters passed to those c",
      "evidence_verified": true
    },
    {
      "id": "draw_auto_binds_default_vao",
      "claim": "When drawing with no VAO bound, MGL auto-binds a default VAO instead of generating GL_INVALID_OPERATION as required for core profile draw/modify/query of vertex array state.",
      "code_locus": "MGL/src/draw_buffers.c:validate_vao",
      "code_excerpt": "if (!vao) { default_vao = mglGetOrCreateDefaultVAO(ctx); STATE(vao) = default_vao; ... }",
      "spec_section": "10.3.1 / 10.4",
      "spec_quote": "An INVALID_OPERATION error is generated by any commands which modify, draw from, or query vertex array state when no vertex array is bound. An INVALID_OPERATION error is generated if no vertex array object is bound.",
      "evidence": "VertexArrays, or if such a name has since been deleted with DeleteVertexArrays. An INVALID_OPERATION error is generated by any commands which modify, draw from, or query vertex array state when no vertex array is bound. This occurs in the initial GL state, and may occur as a result of BindVertexArray or a side effect of DeleteVertexArrays. Vertex array objects may also be created with the command void CreateVertexArrays( sizei n, uint *arrays ); CreateVertexArrays returns n previously unused vertex array object names in arrays, each representing a state vector compr",
      "evidence_verified": true
    }
  ],
  "spec_corpus": [
    "scratch/spec_text/glspec46.core.norm.txt",
    "scratch/spec_text/GLSLangSpec.4.60.norm.txt"
  ]
}
